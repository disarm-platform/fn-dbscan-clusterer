provider:
  name: faas
  gateway: https://faas.srv.disarm.io
functions:
  fn-dbscan-clusterer:
    lang: dockerfile
    handler: ./fn-dbscan-clusterer
    image: disarm/fn-dbscan-clusterer:0.0.12
    environment:
      exec_timeout: 60
      write_timeout: 70
      read_timeout: 70
      combine_output: false
      # write_debug: true